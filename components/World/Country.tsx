import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

type Props = {};

const Country: React.FC<Props> = ({}) => {
  return <Wrapper>Country</Wrapper>;
};

export default Country;
