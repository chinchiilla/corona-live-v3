import React from "react";
import styled from "styled-components";

const Wrapper = styled.div``;

type Props = {};

const MockChart: React.FC<Props> = ({}) => {
    return <Wrapper>MockChart</Wrapper>;
};

export default MockChart;
